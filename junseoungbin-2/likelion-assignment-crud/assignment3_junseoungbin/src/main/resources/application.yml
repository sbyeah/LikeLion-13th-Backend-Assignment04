spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:mysql://localhost:3306/assignment3_jun
    username: root
    password: SAVE!GOOD9=1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
